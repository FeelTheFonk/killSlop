name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  release:
    name: Semantic Release & Publish
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Required for changelog generation

      - name: Calculate Version & Tag
        id: tag_version
        uses: mathieudutour/github-tag-action@v6.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: patch
          release_branches: main
          # This action automatically calculates the next version based on commits (fix->patch, feat->minor, BREAKING->major)
          # It outputs 'new_tag' and 'changelog' for the next step.
          dry_run: false # We want to push the tag if a release is warranted.

      - name: Package Artifacts
        if: steps.tag_version.outputs.new_tag # Only run if a new tag was created
        run: |
          mkdir release_pkg
          # Production files only — CLAUDE.md and .gitignore are intentionally excluded.
          cp 1_prepare_safemode.ps1 release_pkg/
          cp 2_kill_defender.ps1 release_pkg/
          cp 3_verify_status.ps1 release_pkg/
          cp README.md release_pkg/
          cp PSScriptAnalyzerSettings.psd1 release_pkg/
          
          # Zip it up cleanly
          cd release_pkg
          zip -r ../killSlop_${{ steps.tag_version.outputs.new_tag }}.zip .
          cd ..

      - name: Generate SHA256 Checksum
        if: steps.tag_version.outputs.new_tag
        run: |
          sha256sum killSlop_${{ steps.tag_version.outputs.new_tag }}.zip \
            > killSlop_${{ steps.tag_version.outputs.new_tag }}.zip.sha256
          cat killSlop_${{ steps.tag_version.outputs.new_tag }}.zip.sha256

      - name: Create GitHub Release
        if: steps.tag_version.outputs.new_tag # Only run if a new tag was created
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag_version.outputs.new_tag }}
          name: Release ${{ steps.tag_version.outputs.new_tag }}
          body: ${{ steps.tag_version.outputs.changelog }}
          files: |
            killSlop_${{ steps.tag_version.outputs.new_tag }}.zip
            killSlop_${{ steps.tag_version.outputs.new_tag }}.zip.sha256
          token: ${{ secrets.GITHUB_TOKEN }}
